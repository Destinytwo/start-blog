import type { AnnouncementConfig } from "../types/config";

export const announcementConfig: AnnouncementConfig = {
	title: "公告",
	content: "一切刚刚结束，一切刚刚开始。",
	closable: true,
	link: {
		enable: false,
		text: "",
		url: "",
		external: false,
	},
};
